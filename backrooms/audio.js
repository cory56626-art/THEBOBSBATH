export class Sound{
 constructor(){this.ctx=null;this.volume=.45;this.lastStep=0;this.lastPulse=0;}
 start(){try{if(this.ctx){this.ctx.resume();return}this.ctx=new (window.AudioContext||window.webkitAudioContext)();this.master=this.ctx.createGain();this.master.gain.value=this.volume*.28;this.master.connect(this.ctx.destination);this.hum=this.ctx.createGain();this.hum.gain.value=.07;this.hum.connect(this.master);for(const f of [55,110,164.7]){let o=this.ctx.createOscillator(),g=this.ctx.createGain();o.frequency.value=f;o.type='sine';g.gain.value=f===55?.65:.12;o.connect(g);g.connect(this.hum);o.start()}this.noise=this.ctx.createBuffer(1,this.ctx.sampleRate*2,this.ctx.sampleRate);let a=this.noise.getChannelData(0);for(let i=0;i<a.length;i++)a[i]=Math.random()*2-1;}catch{}}
 tone(freq,duration=.15,volume=.2,type='sine'){if(!this.ctx)return;const t=this.ctx.currentTime,o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.setValueAtTime(freq,t);o.frequency.exponentialRampToValueAtTime(Math.max(20,freq*.55),t+duration);g.gain.setValueAtTime(.001,t);g.gain.linearRampToValueAtTime(volume,t+.012);g.gain.exponentialRampToValueAtTime(.001,t+duration);o.connect(g);g.connect(this.master);o.start(t);o.stop(t+duration+.02);}
 noiseHit(duration=.1,volume=.15,cutoff=250){if(!this.ctx)return;let t=this.ctx.currentTime,s=this.ctx.createBufferSource(),f=this.ctx.createBiquadFilter(),g=this.ctx.createGain();s.buffer=this.noise;f.type='lowpass';f.frequency.value=cutoff;g.gain.setValueAtTime(volume,t);g.gain.exponentialRampToValueAtTime(.001,t+duration);s.connect(f);f.connect(g);g.connect(this.master);s.start(t,Math.random());s.stop(t+duration)}
 update(p,state,now){if(!this.ctx)return;this.master.gain.setTargetAtTime(this.volume*.28,this.ctx.currentTime,.1);this.hum.gain.setTargetAtTime(state.night?.018:.07,this.ctx.currentTime,.8);if(p?.moving&&!p.down&&p.y<.1&&now-this.lastStep>(p.stamina<99?.31:.43)){this.lastStep=now;this.noiseHit(.13,.32,210);this.tone(85,.09,.1)}const near=state.enemies.reduce((a,e)=>Math.min(a,Math.hypot(p.x-e.x,p.z-e.z)),99);if(near<10&&now-this.lastPulse>.75){this.lastPulse=now;this.tone(53,.24,(10-near)/18);}}
 cue(type){if(type==='pickup')this.tone(580,.1,.12);if(type==='build')this.noiseHit(.15,.4,500);if(type==='hurt'){this.noiseHit(.21,.3,140);this.tone(42,.3,.4)}if(type==='night'){this.tone(140,1.2,.3);setTimeout(()=>this.tone(88,1,.25),500)}if(type==='radio'){this.noiseHit(.055,.12,1800);this.tone(830,.045,.04)}}
}
